---
type Props = {
  label: string;
  id: string;
};

const { label, id } = Astro.props;
---

<div class="flex flex-col gap-2">
  <label for={id} class="text-muted-foreground text-sm">{label} </label>
  <slot />
</div>

---
import type { Polymorphic } from "astro/types";
import Box from "./Box.astro";

type Props = {} & Polymorphic<{ as: "main" }>;

const props = Astro.props;
---

<Box as="main" {...props}>
  <slot />
</Box>

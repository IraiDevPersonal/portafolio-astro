---
const { href } = Astro.props;
const isActive = href === Astro.url.pathname;
---

<a
  href={href}
  class="underline-offset-8 hover:text-muted-foreground transition-colors"
  class:list={{ underline: isActive }}
>
  <slot />
</a>
